name: core-refactor
id: core-refactor
order: 36
category: quality
description: >
  Executes AUDIT-PLAN.md fix-by-fix (CRITICAL → HIGH → MEDIUM → LOW). All rewritten files go
  into a [ID]-REFACTOR/ sandbox folder — originals are never touched. Produces a SWAP-GUIDE.md
  with exact commands to apply changes when approved. Never run without a completed AUDIT-PLAN.md.
